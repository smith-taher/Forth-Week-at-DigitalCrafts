// code not working
var positiveNumbers = function(numbers) {
    var NegativeNumber = 0;
    if (numbers < 0) {
        numbers = NegativeNumber;
        numbers.splice(NegativeNumber);
    return numbers;
    }

};